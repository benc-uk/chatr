{
  "name": "chatr-server",
  "version": "0.0.3",
  "description": "Server and backend API for chatr application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint . && prettier --check *.js",
    "lint-fix": "eslint . --fix && prettier --write *.js"
  },
  "author": "Ben Coleman",
  "license": "MIT",
  "dependencies": {
    "@azure/data-tables": "^12.0.0-beta.2",
    "@azure/web-pubsub": "^1.0.0-beta.1",
    "@azure/web-pubsub-express": "^1.0.0-beta.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "eslint": "^7.25.0",
    "prettier": "^2.2.1"
  }
}
